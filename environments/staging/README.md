# Staging

No staging Terraform state is owned here yet. Add independent roots only when staging resources exist; shared implementation must be consumed from `modules/` rather than copied from production.
