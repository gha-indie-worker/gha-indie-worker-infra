# GCP module

Provider-specific GCP resources for `gha-indie-worker`. Provider configuration, state/backend configuration, and environment composition belong in `environments/`; this module owns only GCP resource declarations and module inputs/outputs.
