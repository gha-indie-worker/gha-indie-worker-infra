# Neon module

Provider-specific Neon project/database/role declarations. Environment-level provider configuration and state live under `environments/`. The existing `neon/` directory remains the native desired-state/evidence surface during migration.
