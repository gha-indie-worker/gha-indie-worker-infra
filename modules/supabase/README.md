# Supabase module

Terraform ownership for Supabase projects is opt-in until the already-provisioned canonical/auth projects are imported. Native Supabase CLI config, migrations, policies, functions, and project evidence remain under `supabase/`; SQL/schema authority remains outside this infra module.
