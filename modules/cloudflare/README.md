# Cloudflare module

Provider-specific Cloudflare DNS, Access, WAF/cache, and edge-ingress resources. Worker source/config stays under `cloudflare/edge-router`; provider and state configuration stays under `environments/`.
